<div id="<%= dom_id article %>">
  <p>
    <strong>Title:</strong>
    <%= article.title %>
  </p>

  <p>
    <strong>Body:</strong>
    <%= article.body %>
    <%= cl_image_tag(article.photo.key,
      width: 400, height: 300, crop: :fill) %>
  </p>

</div>
